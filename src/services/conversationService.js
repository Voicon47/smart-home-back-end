// const getAllConversation

const saveMessage = async (conversationId, senderId, content) => {
 
}
const getAllConversationByUserId = async () => {
    
}

export const conservationService = {
    saveMessage
}